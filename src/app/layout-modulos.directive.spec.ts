import { LayoutModulosDirective } from './layout-modulos.directive';

describe('LayoutModulosDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutModulosDirective();
    expect(directive).toBeTruthy();
  });
});
